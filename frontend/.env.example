# URL of the backend API. Leave empty to call same-origin "/api"
# (works with the provided nginx reverse proxy / docker-compose setup).
# For a separately hosted backend, set the full origin, e.g.:
#   REACT_APP_BACKEND_URL=https://api.tudominio.com
REACT_APP_BACKEND_URL=
